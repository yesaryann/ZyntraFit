# Essential packages for Fitness AI Coach

# Streamlit framework (MUST HAVE)
streamlit>=1.28.0

# Computer Vision - ONLY use headless version for Streamlit Cloud
opencv-python-headless==4.9.0.80
mediapipe==0.10.14

# Core data science (from your list)
numpy==1.23.5
pandas==2.2.2
matplotlib==3.9.0
seaborn==0.13.2
scipy==1.13.1
scikit-learn==1.5.0

# Utilities (essential ones from your list)
joblib==1.4.2
tqdm==4.66.4
packaging==24.0
python-dateutil==2.9.0.post0
pytz==2024.1
requests==2.32.1
protobuf==4.25.3

# For chatbot functionality (if you need it)
langchain>=0.1.0
langchain-openai>=0.0.5
openai>=1.0.0
python-dotenv>=0.19.0

# Image processing
Pillow>=9.0.0

# Optional: Only add if your ExerciseAiTrainer specifically needs TensorFlow
# WARNING: This may cause deployment failures due to size
# tensorflow-cpu==2.16.1

# Optional: Only if you use audio features
# sounddevice==0.4.6
